services:
  rabbitmq:
    image: rabbitmq:4.0-management
    container_name: rabbitmq_production
    hostname: localhost
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=admin
      - RABBITMQ_DEFAULT_PASS=SuperSecurePassword123!
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "6379:6379"

volumes:
  rabbitmq_data: